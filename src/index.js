import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
//This is going to trigger div with an id of root and going to inject entire react application inside of it.
ReactDOM.render(<App />, document.getElementById("root"));